import streamlit as st

# واجهة المهندس بيلان عبد الكريم
st.set_page_config(page_title="Bilan Engineering", layout="wide")

st.title("🏗️ Bilan Engineering Suite")
st.subheader("إعداد: المهندس بيلان عبد الكريم")

# القائمة الجانبية للمدخلات
with st.sidebar:
    st.header("معطيات التصميم")
    elem = st.selectbox("اختر العنصر:", ["جائز", "بلاطة", "أساسات", "أعمدة"])
    L = st.number_input("الطول L (متر):", 1.0, 15.0, 5.0)
    h = st.number_input("السماكة h (سم):", 10.0, 150.0, 60.0)
    wu = st.number_input("الحمل Wu (طن/م):", 0.1, 50.0, 2.5)

# الحسابات الأساسية
fcu = 250
fy = 4000
d = h - 5

# عرض النتائج بناءً على الاختيار
if elem == "جائز" or elem == "بلاطة":
    Mu = (wu * L**2) / 8
    As = (Mu * 100000) / (0.87 * fy * d)
    
    st.success(f"النتيجة: العزم التصميمي = {Mu:.2f} طن.متر")
    st.info(f"مساحة التسليح المطلوبة As = {As:.2f} سم2")
    
    if elem == "جائز":
        st.write("### تفريد حديد الجائز")
        
    else:
        st.write("### تسليح البلاطة")
        

if elem == "أساسات":
    area = (wu / 2.0) * 1.1
    side = area**0.5
    st.success(f"مساحة القاعدة المطلوبة: {area:.2f} م2")
    st.info(f"الأبعاد المقترحة: {side:.2f} x {side:.2f} متر")
    

if elem == "أعمدة":
    capacity = (0.35 * fcu * 30 * h + 0.67 * fy * 0.01 * 30 * h) / 1000
    st.success(f"قدرة تحمل العمود التقريبية: {capacity:.1f} طن")
    

st.divider()
st.write("✅ المذكرة الحسابية معتمدة من المهندس بيلان عبد الكريم")
