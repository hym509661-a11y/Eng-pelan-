import datetime

def structural_check():
    print("--- 🏗️ نظام التصميم الإنشائي الذكي (الكود السوري) ---")
    
    # مدخلات المهندس
    eng_name = input("✍️ أدخل اسم المهندس: ")
    date_now = datetime.date.today()
    
    print(f"\nمرحباً مهندس {eng_name} | التاريخ: {date_now}")
    print("-" * 40)
    
    # مدخلات المشروع
    n_floors = int(input("🏢 عدد الطوابق (قبو + أرضي + متكرر): "))
    b = float(input("📏 عرض العمود b (cm): "))
    h = float(input("📏 ارتفاع العمود h (cm): "))
    
    # 1. نظام الإنذار (مساحة المقطع)
    area = b * h
    if area < 900:
        print("\n🚨🚨 إنذار خطير 🚨🚨")
        print(f"المقطع {b}x{h} مساحته {area}cm² (أقل من الحد الأدنى 900cm² وفق الكود السوري)")
    else:
        print(f"\n✅ المقطع {area}cm² مطابق لاشتراطات الكود.")

    # 2. حساب قدرة التحمل والتحقق من الطوابق
    fcu = 250  # إجهاد الخرسانة
    fy = 4000  # إجهاد الحديد
    capacity = (0.35 * fcu * area + 0.67 * fy * (0.01 * area)) / 1000 # بالطن
    estimated_load = n_floors * 45  # تقدير 45 طن لكل طابق
    
    if capacity < estimated_load:
        print(f"❌ إنذار حمولة: العمود لا يتحمل {n_floors} طوابق! (القدرة: {capacity:.1f}t < الحمل: {estimated_load}t)")
    else:
        print(f"👍 القدرة الإنشائية كافية ({capacity:.1f} طن).")

    # 3. حسابات الهوردي
    L = float(input("\n📏 أدخل أطول بحر في الغرفة (m): "))
    t_hordy = round((L * 100) / 21)
    print(f"🧱 سماكة بلاطة الهوردي المقترحة: {t_hordy} cm")

    # 4. المذكرة الحسابية النهائية
    print("\n" + "="*40)
    print(f"📋 المذكرة الحسابية للمشروع")
    print(f"المصمم: المهندس {eng_name}")
    print(f"التاريخ: {date_now}")
    print(f"المعطيات: fcu={fcu}, fy={fy}")
    print(f"النتيجة: مقطع {b}x{h} سم | سماكة بلاطة {t_hordy} سم")
    print("="*40)

structural_check()
