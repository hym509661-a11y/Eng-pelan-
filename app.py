import streamlit as st

# واجهة المهندس بيلان عبد الكريم
st.set_page_config(page_title="Bilan-Pro", layout="wide")

st.title("🏗️ Bilan Engineering Suite")
st.subheader("إعداد: المهندس بيلان عبد الكريم")

# القائمة الجانبية للمدخلات
elem = st.sidebar.selectbox("اختر العنصر:", ["جائز", "بلاطة", "أساس", "عمود"])
L = st.sidebar.number_input("الطول L (متر):", 1.0, 15.0, 5.0)
h = st.sidebar.number_input("السماكة h (سم):", 10.0, 150.0, 60.0)
wu = st.sidebar.number_input("الحمل Wu (طن):", 0.1, 500.0, 2.5)
phi = st.sidebar.selectbox("القطر (مم):", [12, 14, 16, 20])

# الحسابات (بنية بسيطة جداً لمنع أخطاء الإزاحة)
fcu = 250
fy = 4000
d = h - 5

# حساب النتائج بناء على نوع العنصر
if elem == "جائز" or elem == "بلاطة":
    Mu = (wu * L**2) / 8
    As = (Mu * 100000) / (0.87 * fy * d)
    area_bar = (3.14 * (phi/10)**2) / 4
    n_bars = int(As / area_bar) + 1
    
    st.success(f"النتيجة: العزم = {Mu:.2f} طن.متر")
    st.info(f"عدد الأسياخ المطلوب: {max(n_bars, 2)} T{phi}")
    
    if elem == "جائز":
        st.write("### تفريد حديد الجائز")
        
    else:
        st.write("### تسليح البلاطة (فرش وغطاء)")
        

if elem == "أساس":
    area = (wu / 2.0) * 1.1
    side = area**0.5
    st.success(f"مساحة القاعدة المطلوبة: {area:.2f} م2")
    st.info(f"الأبعاد المقترحة: {side:.2f} x {side:.2f} متر")
    

if elem == "عمود":
    capacity = (0.35 * fcu * 30 * h + 0.67 * fy * 0.01 * 30 * h) / 1000
    st.success(f"قدرة تحمل العمود التقريبية: {capacity:.1f} طن")
    

st.divider()
st.write("✅ المذكرة الحسابية معتمدة من المهندس بيلان عبد الكريم")
